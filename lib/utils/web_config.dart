/// Cấu hình cho ứng dụng web
///
/// Để chạy web với port cố định, sử dụng:
/// flutter run -d chrome --web-port=5000
/// hoặc
/// flutter run -d web --web-port=5000
library;

const String DEFAULT_WEB_PORT = '5000';
const String WEB_URL = 'http://localhost:5000';
